/**
 * Tool widgets re-export module
 *
 * This file re-exports all widgets from the widgets directory to maintain
 * backward compatibility with the original ToolWidgets.tsx structure.
 * Provides a clean import path for all tool-specific widget components.
 *
 * @example
 * ```tsx
 * import { BashWidget, LSWidget, TodoWidget } from '@/components/ToolWidgets.new';
 * ```
 */

// eslint-disable-next-line react-refresh/only-export-components
export * from "./widgets";
