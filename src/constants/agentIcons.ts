import { ICON_MAP } from "@/constants/iconConstants";

/**
 * Available icons for agents
 *
 * This constant is exported separately to avoid fast refresh warnings
 * when importing in React components.
 */
export const AGENT_ICONS = ICON_MAP;
